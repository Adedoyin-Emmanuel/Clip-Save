import React from "react";

const NotFound = (): JSX.Element => {
  return (
    <React.Fragment>
      <section className="container-fluid">looks like you are lost</section>
    </React.Fragment>
  );
};

export default NotFound;
