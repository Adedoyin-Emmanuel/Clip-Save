import React from "react";

const Support = (): JSX.Element => {
  return (
    <React.Fragment>
      <section className="container-fluid">support section.</section>
    </React.Fragment>
  );
};

export default Support;
