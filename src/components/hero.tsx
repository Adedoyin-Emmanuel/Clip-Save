import React from "react";

const AppHero = (): JSX.Element => {
  return (
    <React.Fragment>
      <section className="hero-section">app hero</section>
    </React.Fragment>
  );
};

export default AppHero;
