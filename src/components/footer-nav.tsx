import React from "react";

const AppFooterNav = (): JSX.Element => {
  return (
    <React.Fragment>
      <section className="footer-nav-section">footer nav section</section>
    </React.Fragment>
  );
};

export default AppFooterNav;
